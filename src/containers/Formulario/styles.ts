import styled from 'styled-components'
import { Campo } from '../../styles'

export const Form = styled.form`
  max-width: 547px;
  width: 100%;
  font-weight: bold;
  font-size: 14px;
  color: #666666;

  ${Campo} {
    margin-bottom: 16px;
  }
`
